name: Ingest remaining structural projects
on:
  push:
    branches: [main]
    paths:
      - 'Linkoteq_remaining_structural_projects_part1.zip'
      - 'Linkoteq_remaining_structural_projects_part2.zip'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Extract remaining structural projects
        run: |
          mkdir -p datasets/validated-llm-v0.1
          unzip -o Linkoteq_remaining_structural_projects_part1.zip -d datasets/validated-llm-v0.1
          unzip -o Linkoteq_remaining_structural_projects_part2.zip -d datasets/validated-llm-v0.1
          rm Linkoteq_remaining_structural_projects_part1.zip Linkoteq_remaining_structural_projects_part2.zip
      - name: Commit extracted projects
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "Ingest remaining structural projects"
          git push

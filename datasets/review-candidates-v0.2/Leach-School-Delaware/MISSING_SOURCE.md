# Missing source files — candidate review

This project is quarantined in `datasets/review-candidates-v0.2` because no reviewable PDF/PNG project files are present in `datasets/validated-llm-v0.1` on `main`. It must not be treated as validated or training-ready. Recover the authoritative source drawing, then perform content-based visual QA under the active dataset admission contract before admission.
